[package]
name = "formal-svm-system-allocate-assign-caller"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2.1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
