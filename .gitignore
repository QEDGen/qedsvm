.lake/
build/
# docs/ is local scratch space by default; the public reference docs are
# tracked explicitly. `docs/*` (not `docs/`) so the negations below re-include.
docs/*
!docs/API.md
!docs/PIPELINE.md
!docs/COVERAGE.md
rust-bridge/target/
.agents/
.claude/
