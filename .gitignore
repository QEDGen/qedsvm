.lake/
build/
# docs/ is local scratch space by default; the public reference docs are
# tracked explicitly. `docs/*` (not `docs/`) so the negations below re-include.
docs/*
!docs/API.md
!docs/PIPELINE.md
!docs/COVERAGE.md
!docs/REFINEMENT_DESCRIPTOR.md
rust-bridge/target/
.agents/
.claude/
# Local research spikes (e.g. solanalib-diff-oracle) — not tracked.
spike/
