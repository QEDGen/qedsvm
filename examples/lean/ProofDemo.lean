import ByteIncrement

/-!
# Proof demo

Entry point: `lake build ProofDemo`. A green build = a binary proved to meet its spec (no `sorry`).
Theorem lives in `examples/lean/ByteIncrement.lean` (4-insn ldx+add64+stx+exit end-to-end).
-/
